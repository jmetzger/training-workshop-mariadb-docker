# Zugriff auf mariadb - docker - container 

## Walkthough 

```
docker container ls
docker exec -it mariadb-mariadb-1 bash
```

```
mysql -uroot -p<vergebene passwort bei installation über docker-compose.yaml>
```

